function showResult()
{
    document.getElementById("resultDiv").innerHTML = '<pre><code>#include &lt;stdio.h&gt;\n#include &lt;math.h&gt;\n\nint main()\n{\n    long int cc;\n    int digit_count = 0;\n    int sum = 0;\n\n    // Asks user for input of card number\n    do\n    {\n        printf("Enter card number: ");\n        scanf("%ld", &cc);\n    }\n    while (cc < 1);\n\n    // Checksum for the Luhn algorithm\n    while (cc > 0)\n    {\n        int digit = cc % 10;\n        cc /= 10;\n        digit_count++;\n\n        if (digit_count % 2 == 0)\n        {\n            digit *= 2;\n            if (digit > 9)\n            {\n                digit -= 9;\n            }\n        }\n\n        sum += digit;\n    }\n\n    // Statements to check if the card is valid or invalid\n    // If the result of sum ends with 0, then the card is valid\n    if (sum % 10 == 0)\n    {\n        printf("Valid credit card\\n");\n    }\n    else\n    {\n        printf("Invalid credit card\\n");\n    }\n\n    // Terminate the program\n    return 0;\n}</code></pre>';
}